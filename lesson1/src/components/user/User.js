import React from 'react';

const User = ({name, age, email}) => {
    return (
        <div>
            <p>name: {name}</p>
            <p>name: {age}</p>
            <p>name: {email}</p>
        </div>
    );
};

export default User;