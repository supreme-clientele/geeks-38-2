// rsc
import React from 'react';
import Title from "../title/Title";

const Header = () => {
    return (
        <div>
            <Title name = {'Header'}/>
        </div>
    );
};

export default Header;